fn main() {
    println!("iopool benchmark is parameterized and uses an external data file.");
    println!(
        "run: cargo run --release -p scdata-rust --example iopool_bench -- --path <FILE> [options]"
    );
}
